---
description: A relentless interview to sharpen a plan or design.
---

Load the `grilling` skill with the `skill` tool. Use the current conversation and $ARGUMENTS as the subject, then follow that skill exactly.
